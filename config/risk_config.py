from __future__ import annotations

"""Risk / Money Management 관련 전역 설정값

필요에 따라 값(한도)을 조정하여 사용할 수 있다.
"""

# ---------------------------------------------------------------------------
# 1) Stop-Day (일일 손실 한도)
# ---------------------------------------------------------------------------
# 당일 누적 실현 PnL 이 –DAILY_LOSS_LIMIT_KRW 이하로 내려가면 추가 매수를 중단한다.
# ※ 음수(손실) 값으로 비교하므로 절대값만 입력하면 된다.
DAILY_LOSS_LIMIT_KRW: float = 100_000  # 10만원

# ---------------------------------------------------------------------------
# 2) 자산 비중 한도 (코인 비중)
# ---------------------------------------------------------------------------
# 전체 자산(코인 평가액 + 현금) 중 코인이 차지할 수 있는 최대 비율 (0.0 ~ 1.0)
# ex) 0.7 → 최대 70% 까지만 코인 보유 허용
MAX_COIN_RATIO: float = 0.7 